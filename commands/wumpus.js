module.exports = {
    name: 'wumpus',
    description: 'Play Hunt the Wumpus!',
    async execute(client, message) {
        message.channel.send('Tell Mac to not get lazy when coding this.')
    }
}